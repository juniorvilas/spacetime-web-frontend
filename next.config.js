/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['avatars.githubusercontent.com', 'localhost', 'aws-spacetime-bucket.s3.amazonaws.com'],
    },
  }
  
  module.exports = nextConfig
